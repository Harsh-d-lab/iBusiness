import * as React from 'react';

const Home = () => {
    const data = {
      name: "thepowerhouse.com",
    };

    return (
        <>
          myData={data}
        </>
      );
    };
    export default Home;